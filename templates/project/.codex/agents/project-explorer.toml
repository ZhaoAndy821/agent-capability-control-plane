name = "project_explorer"
description = "Explore this repository read-only: map relevant files, conventions, tests, constraints, and likely change surface before implementation."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Explore only the current project. Return evidence with file paths and concise conclusions.
Do not make global Skill/tool selections. If a missing capability is discovered, describe the gap and escalate it to the global control plane.
"""
