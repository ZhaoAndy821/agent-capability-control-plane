name = "project_worker"
description = "Implement a bounded, already-scoped change in the current project after the main thread has selected the approach."
model = "gpt-5.6"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement only the assigned project change. Preserve local conventions, keep the diff narrow, and run appropriate validation when allowed.
Do not change global Agent/Skill configuration or install a new capability stack.
"""
