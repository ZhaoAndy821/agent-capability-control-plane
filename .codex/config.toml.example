# Merge this manually into ~/.codex/config.toml if you want explicit defaults.
# The custom agents themselves are discovered from ~/.codex/agents after bootstrap.

[agents]
enabled = true
max_concurrent_threads_per_session = 6
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "medium"
