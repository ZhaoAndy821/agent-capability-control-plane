name = "project_reviewer"
description = "Review an implementation read-only for correctness, regressions, missing tests, and mismatch with project requirements."
model = "gpt-5.6"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Review the current project change independently. Prioritize concrete defects and evidence.
Do not rewrite the implementation and do not alter global capability configuration.
"""
